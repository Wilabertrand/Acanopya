<div class="container py-4">
<%= render 'shared/tab' %>
  <%# <div class="pb-4">
    <div class="subtitle"><h1>Nos hébergements</h1></div>
  </div>   %>
  <div class="container pt-4 pb-1">
    <%= simple_form_for :search, url: trip_flats_path, method: 'GET', html: { class: 'form-inline' } do |f| %>
      <%= f.input :price_min, placeholder: 'prix minimum', label: false, input_html: {value: ''} %>
      <%= f.input :price_max, placeholder: 'prix maximum', label: false, input_html: {value: ''} %>
      <%= f.submit "Search", class: "btn btn-primary" %>
    <% end %>
  <div class="content">
    <div class="cards">
      <% @flats.each do |flat| %>
       <%= link_to trip_flat_path(trip_id: @trip.id, id: flat.id) do %>
          <div class="card-trip">  
            <%= image_tag 'photo.jpg' %>
            <div class="card-trip-infos">
              <div>
                <p><%= flat.address %></p>
                <h2 class="pb-2 mb-2 border-bottom"><%= flat.name %></h2>
                <p>label : <%= flat.label %></p>
                <p>pour <%= flat.capacity %> personnes</p>
              </div>
              <h2 class="card-trip-pricing"><%= flat.price %>€</h2>
              <img src="https://kitt.lewagon.com/placeholder/users/krokrob" class="card-trip-user avatar-bordered"/>
            </div>
          </div> 
       <% end %> 
      <% end %>
    </div>
    <div id="trip-box" style="width: 300px; height: 100vh;">
      <ul class="list-inline t-tabs-underlined">
        <li>
          <%= link_to "Carte", trip_flats_path(@trip.id), class: "t-tab-underlined active" %>
        </li>
        <li>
          <%= link_to "Voyage", trip_restaurants_path(@trip.id), class: "t-tab-underlined" %>
        </li>
      </ul>
      <div id="map"
        style="width: 300px; height: 100vh;"
        data-markers="<%= @markers.to_json %>"
        data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
      </div>
>>>>>>> master
    </div>
  </div>
</div>





