<div class="container">
	<h1>Tous Mes voyages</h1>
	<ul>
		<% @trips.each do |trip| %>
			<li><%= link_to trip.name, trip_path(trip) %></li>
			<li><% if policy(trip).edit? %>
			<%= link_to "Editer", edit_trip_path(trip) %></li>
		<% end %>
	</ul>
</div>